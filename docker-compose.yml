services:
  canton-alert-bot:
    build: .
    container_name: canton-alert-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /data/bot.db
      NODE_ENV: production
    volumes:
      - bot_data:/data

volumes:
  bot_data:
